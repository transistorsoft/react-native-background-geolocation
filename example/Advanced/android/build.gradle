buildscript {
    ext {
        buildToolsVersion = "36.0.0"
        minSdkVersion = 24
        compileSdkVersion = 36
        targetSdkVersion = 36
        ndkVersion = "27.1.12297006"
        kotlinVersion = "2.1.20"
        tslocationmanagerVersion = "4.0.+"
    }
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath("com.android.tools.build:gradle")
        classpath("com.facebook.react:react-native-gradle-plugin")
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin")
    }
}

// development
allprojects {
    // background_geolocation
    // [DEV] Sonatype SNAPSHOT url
    repositories {
        maven { url "https://central.sonatype.com/repository/maven-snapshots/" }
    }

    configurations.all {
        resolutionStrategy {
            cacheChangingModulesFor(0, "seconds")
            cacheDynamicVersionsFor(0, "seconds")
        }
    }
}

/*
allprojects { p ->
  p.repositories.whenObjectAdded { repo ->
    if (repo instanceof MavenArtifactRepository) {
      def u = repo.url?.toString() ?: ""
      if (u.contains("central.sonatype.com/repository/maven-snapshots")) {
        println "🚨 Added Sonatype snapshots repo"
        println "   project: ${p.path}"
        println "   script : ${p.buildFile}"
      }
    }
  }
}
*/

apply plugin: "com.facebook.react.rootproject"
